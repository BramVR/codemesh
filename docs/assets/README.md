---
summary: "Committed docs-site visual identity assets and screenshot proof."
read_when:
  - "Changing docs-site visual assets, social cards, favicons, or screenshot proof."
---

# Docs Site Assets

`codemesh-hero.svg`, `codemesh-workspace-fabric.png`, `social-card.svg`, and `social-card.png` are copied into the local docs-site artifact.

`codemesh-workspace-fabric.png` is the generated homepage hero visual: a dark product illustration of local project inventory, readiness signals, machine nodes, and temporary agent workspaces.

`social-card.png` is the Open Graph and Twitter preview image. The SVG source remains committed for editing, but major social platforms do not render SVG preview images reliably.

`docs-site-desktop.png` and `docs-site-mobile.png` are committed visual proof for the #48 local docs-site tracer bullet PR. They are intentionally not copied into the generated site.
